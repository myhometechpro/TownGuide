import type {Metadata} from "next";import {PageHero} from "@/components/page-hero";import {LodgingDirectory} from "@/components/lodging-directory";import {getBusinesses} from "@/lib/businesses";
export const metadata:Metadata={title:"Places to Stay"};export default async function Page(){const stays=(await getBusinesses()).filter(x=>x.category.toLowerCase()==="lodging");return <><PageHero eyebrow="Your mountain basecamp" title="Places to stay" text="Browse cabins, hotels, vacation rentals, RV options, and camping listings as verified partners join the guide."/><section className="mx-auto max-w-7xl px-5 py-14 lg:px-8"><LodgingDirectory stays={stays}/></section></>}
