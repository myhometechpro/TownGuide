import type {Metadata} from "next";import {PageHero} from "@/components/page-hero";import {BusinessCard} from "@/components/cards";import {businesses} from "@/lib/data";
export const metadata:Metadata={title:"Places to Stay"};export default function Page(){const stays=businesses.filter(x=>x.category==="Lodging");return <><PageHero eyebrow="Your mountain basecamp" title="Places to stay" text="Browse cabins, hotels, vacation rentals, RV options, and camping listings as verified partners join the guide."/><section className="mx-auto max-w-7xl px-5 py-14 lg:px-8"><div className="mb-8 flex flex-wrap gap-2">{["Cabins","Hotels","Vacation Rentals","RV","Camping"].map(x=><span className="rounded-full bg-white px-4 py-2 text-sm font-bold" key={x}>{x}</span>)}</div><div className="grid gap-5 md:grid-cols-3">{stays.map(x=><BusinessCard item={x} key={x.id}/>)}</div></section></>}
