import type {MetadataRoute} from "next";import {businesses} from "@/lib/data";export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_SITE_URL||"http://localhost:3000";const paths=["","/businesses","/things-to-do","/trails","/events","/deals","/stay","/visitor-info","/map","/privacy"];return [...paths.map(url=>({url:base+url,lastModified:new Date()})),...businesses.map(b=>({url:`${base}/businesses/${b.slug}`,lastModified:new Date()}))]}
