import type {MetadataRoute} from "next";import {businesses} from "@/lib/data";
export default function sitemap():MetadataRoute.Sitemap{const base=(process.env.NEXT_PUBLIC_SITE_URL||"http://localhost:3000").replace(/\/+$/,"");const paths=["","/businesses","/things-to-do","/trails","/events","/deals","/stay","/history","/visitor-info","/map","/privacy","/advertising-terms"];return [...paths.map(url=>({url:base+url,lastModified:new Date()})),...businesses.map(b=>({url:`${base}/businesses/${b.slug}`,lastModified:new Date()}))]}
