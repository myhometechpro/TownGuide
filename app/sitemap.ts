import type {MetadataRoute} from "next";
import {getBusinesses} from "@/lib/businesses";import {getAttractions} from "@/lib/content";import {getPublicVacationRentals} from "@/lib/vacation-rentals";
export default async function sitemap():Promise<MetadataRoute.Sitemap>{const base=(process.env.NEXT_PUBLIC_SITE_URL||"http://localhost:3000").replace(/\/+$/,"");const paths=["","/businesses","/things-to-do","/trails","/events","/deals","/stay","/history","/visitor-info","/map","/contact","/advertise","/advertise/request","/vacation-rentals/list-your-rental","/privacy","/advertising-terms"],[businesses,attractions,rentals]=await Promise.all([getBusinesses(),getAttractions(),getPublicVacationRentals()]);return [...paths.map(url=>({url:base+url,lastModified:new Date()})),...businesses.map(b=>({url:`${base}/businesses/${b.slug}`,lastModified:new Date()})),...attractions.map(item=>({url:`${base}/things-to-do/${item.slug}`,lastModified:new Date()})),...rentals.map(item=>({url:`${base}/vacation-rentals/${item.slug}`,lastModified:new Date(item.updated_at)}))]}
