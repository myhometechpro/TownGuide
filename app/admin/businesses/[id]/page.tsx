import Link from "next/link";import {notFound,redirect} from "next/navigation";import {getSupabase} from "@/lib/supabase/server";import {rowToBusiness} from "@/lib/businesses";import {AdminBusinessForm} from "@/components/admin-business-form";import {saveBusiness} from "../actions";
export default async function Page({params}:{params:Promise<{id:string}>}){const db=await getSupabase();if(!db)redirect("/admin/businesses");const {data:{user}}=await db.auth.getUser();if(!user)redirect("/admin/login");const {id}=await params;const {data}=await db.from("businesses").select("id,name,slug,category,short_description,description,address,phone,website,image_url,featured,sponsored,active").eq("id",id).maybeSingle();if(!data)notFound();return <section className="mx-auto max-w-4xl px-5 py-12"><Link href="/admin/businesses" className="text-sm font-bold text-forest">← Businesses</Link><h1 className="mt-5 font-display text-4xl">Edit business</h1><AdminBusinessForm business={rowToBusiness(data)} action={saveBusiness}/></section>}
