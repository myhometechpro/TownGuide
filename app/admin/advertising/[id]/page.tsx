import Link from "next/link";import {notFound} from "next/navigation";import {requireAdmin} from "@/lib/supabase/auth";import {saveCampaign} from "../actions";import {singleRelation,type AdCampaign,type AdProduct} from "@/lib/advertising";
export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params,{db}=await requireAdmin();const [{data},{data:products},{data:events}]=await Promise.all([db.from("ad_campaigns").select("*,businesses(name,slug),ad_products(*)").eq("id",id).single(),db.from("ad_products").select("*").order("name"),db.from("ad_events").select("event_type,created_at").eq("campaign_id",id).order("created_at",{ascending:false})]);if(!data)notFound();const c=data as AdCampaign,p=singleRelation(c.ad_products),impressions=(events||[]).filter(x=>x.event_type==="impression").length,clicks=(events||[]).filter(x=>x.event_type==="click").length,rate=impressions?((clicks/impressions)*100).toFixed(1):"0.0",field="mt-2 h-12 w-full border border-cream/20 bg-[#202a24] px-3 text-cream";return <section className="min-h-screen px-5 py-12"><div className="mx-auto max-w-4xl"><Link href="/admin/advertising" className="font-bold text-forest">← Advertising manager</Link><h1 className="mt-5 font-display text-4xl">Edit campaign</h1><div className="mt-6 grid gap-3 sm:grid-cols-3"><div className="bg-pine p-5 text-cream"><strong className="text-3xl">{impressions}</strong><p className="text-sm text-cream/60">Impressions</p></div><div className="bg-pine p-5 text-cream"><strong className="text-3xl">{clicks}</strong><p className="text-sm text-cream/60">Clicks</p></div><div className="bg-pine p-5 text-cream"><strong className="text-3xl">{rate}%</strong><p className="text-sm text-cream/60">Click rate</p></div></div><form action={saveCampaign} className="mt-6 grid gap-5 bg-white p-6 md:grid-cols-2"><input type="hidden" name="id" value={c.id}/><input type="hidden" name="business_id" value={c.business_id}/><label className="text-sm font-bold">Product<select name="product_id" defaultValue={c.product_id} className={field}>{(products as AdProduct[]||[]).map(x=><option value={x.id} key={x.id}>{x.name}</option>)}</select></label><label className="text-sm font-bold">Status<select name="status" defaultValue={c.status} className={field}>{["draft","pending_payment","scheduled","active","expired","cancelled"].map(x=><option value={x} key={x}>{x.replace("_"," ")}</option>)}</select></label><label className="text-sm font-bold md:col-span-2">Headline<input name="headline" defaultValue={c.headline} required className={field}/></label><label className="text-sm font-bold md:col-span-2">Ad text<textarea name="ad_copy" defaultValue={c.ad_copy||""} className="mt-2 min-h-24 w-full border border-cream/20 bg-[#202a24] p-3 text-cream"/></label><label className="text-sm font-bold">Start date<input type="date" name="start_date" defaultValue={c.start_date} required className={field}/></label><label className="text-sm font-bold">End date<input type="date" name="end_date" defaultValue={c.end_date} required className={field}/></label><label className="text-sm font-bold md:col-span-2">Photo URL<input type="url" name="image_url" defaultValue={c.image_url||""} className={field}/></label><label className="text-sm font-bold md:col-span-2">Destination URL<input type="url" name="destination_url" defaultValue={c.destination_url||""} className={field}/></label><label className="text-sm font-bold md:col-span-2">Payment link<input type="url" name="billing_url" defaultValue={c.billing_url||""} className={field}/></label><label className="text-sm font-bold md:col-span-2">Payment reference<input name="payment_reference" defaultValue={c.payment_reference||""} className={field}/></label><label className="flex items-center gap-2 font-bold"><input type="checkbox" name="paid" defaultChecked={c.paid}/> Payment received</label><label className="flex items-center gap-2 font-bold"><input type="checkbox" name="approved" defaultChecked={c.approved}/> Content approved</label><p className="text-xs text-ink/55 md:col-span-2">A campaign appears publicly only when it is paid, approved, active, and inside its start/end dates. Current product: {p?.name}</p><button className="bg-pine px-6 py-3 font-bold text-cream md:col-span-2">Save campaign</button></form></div></section>}
