"use server";
import {redirect} from "next/navigation";import {requireAdmin} from "@/lib/supabase/auth";
async function copyCampaign(id:string,renew:boolean){const {db}=await requireAdmin(),{data,error}=await db.from("ad_campaigns").select("business_id,product_id,headline,ad_copy,image_url,destination_url,start_date,end_date,billing_price_cents,booked_duration_days,pricing_term").eq("id",id).single();if(error||!data)throw new Error(error?.message||"Campaign not found");const days=data.booked_duration_days||30,base=renew&&data.end_date>new Date().toISOString().slice(0,10)?new Date(`${data.end_date}T12:00:00`):new Date();base.setDate(base.getDate()+(renew?1:0));const start=base.toISOString().slice(0,10),finish=new Date(`${start}T12:00:00`);finish.setDate(finish.getDate()+days-1);const {data:created,error:createError}=await db.from("ad_campaigns").insert({...data,headline:renew?`${data.headline} — Renewal`:`${data.headline} — Copy`,start_date:start,end_date:finish.toISOString().slice(0,10),status:"draft",approved:false,paid:false,billing_url:null,payment_reference:null,stripe_paid_at:null,stripe_checkout_session_id:null,created_at:new Date().toISOString(),updated_at:new Date().toISOString()}).select("id").single();if(createError||!created)throw new Error(createError?.message||"Campaign could not be copied");redirect(`/admin/advertising/${created.id}`)}
export async function duplicateCampaign(form:FormData){await copyCampaign(String(form.get("id")),false)}
export async function renewCampaign(form:FormData){await copyCampaign(String(form.get("id")),true)}
