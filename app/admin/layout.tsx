import Link from "next/link";import {headers} from "next/headers";import {requireAdmin} from "@/lib/supabase/auth";import {AdminSignOut} from "@/components/admin-sign-out";
export default async function AdminLayout({children}:{children:React.ReactNode}){const path=(await headers()).get("x-town-guide-path")||"";const login=path==="/admin/login";if(!login)await requireAdmin();return <>{!login&&<div className="border-b border-cream/10 bg-pine px-5 py-3 text-cream"><div className="mx-auto flex max-w-7xl items-center justify-between"><Link href="/admin" className="font-bold">TownGuide Admin</Link><AdminSignOut/></div></div>}{children}</>}
