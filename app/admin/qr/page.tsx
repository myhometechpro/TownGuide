import {QRGenerator} from "@/components/qr-generator";export default function Page(){return <section className="mx-auto max-w-2xl px-5 py-12"><p className="text-xs font-black uppercase tracking-[.2em] text-forest">Admin tools</p><h1 className="mt-2 font-display text-4xl">QR location generator</h1><p className="mt-3 text-ink/60">Create the database record in the admin workflow, then generate its printable visitor card here.</p><div className="mt-8"><QRGenerator/></div></section>}
