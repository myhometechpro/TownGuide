import {QRGenerator} from "@/components/qr-generator";import {BusinessQrSigns} from "@/components/business-qr-signs";import {requireAdmin} from "@/lib/supabase/auth";import {createBusinessQrLocations} from "./actions";
export default async function Page({searchParams}:{searchParams:Promise<{created?:string}>}){const {db}=await requireAdmin();const [{data:businesses},{data:locations},message]=await Promise.all([db.from("businesses").select("id,name,slug").eq("active",true).order("name"),db.from("qr_locations").select("code,business_id").eq("active",true),searchParams]);const byBusiness=new Map((locations||[]).map(x=>[x.business_id,x.code]));const signs=(businesses||[]).filter(x=>byBusiness.has(x.id)).map(x=>({name:x.name,slug:x.slug,code:byBusiness.get(x.id)!}));return <section className="mx-auto max-w-6xl px-5 py-12"><p className="text-xs font-black uppercase tracking-[.2em] text-forest">Admin tools</p><h1 className="mt-2 font-display text-4xl">Business QR signs</h1><p className="mt-3 max-w-3xl text-ink/60">Create a tracked sign for every published business, then download full-resolution local PNG copies using your approved artwork.</p><form action={createBusinessQrLocations} className="mt-6"><button className="bg-pine px-5 py-3 font-bold text-cream">Create or refresh all business QR codes</button></form>{message.created&&<p className="mt-5 border border-forest/40 bg-forest/15 p-4 font-bold">Business QR locations are ready.</p>}{signs.length?<BusinessQrSigns signs={signs}/>:<p className="mt-8 bg-white p-6">No business signs have been created yet. Use the button above.</p>}<details className="mt-10 border border-cream/10 bg-white p-6"><summary className="cursor-pointer font-display text-2xl">Create a custom location QR</summary><div className="mt-6 max-w-2xl"><QRGenerator/></div></details></section>}
