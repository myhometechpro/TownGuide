import {redirect} from "next/navigation";import {getSupabase} from "@/lib/supabase/server";
export default function Page(){async function login(form:FormData){"use server";const db=await getSupabase();if(!db)return;const {error}=await db.auth.signInWithPassword({email:String(form.get("email")),password:String(form.get("password"))});if(!error)redirect("/admin")}return <section className="grid min-h-[70vh] place-items-center p-5"><form action={login} className="w-full max-w-md rounded-3xl bg-white p-8 shadow-soft"><p className="text-xs font-black uppercase tracking-[.2em] text-forest">Secure access</p><h1 className="mt-3 font-display text-3xl">Admin sign in</h1><label className="mt-7 block text-sm font-bold">Email<input name="email" type="email" required className="mt-2 h-12 w-full rounded-xl border px-3"/></label><label className="mt-4 block text-sm font-bold">Password<input name="password" type="password" required className="mt-2 h-12 w-full rounded-xl border px-3"/></label><button className="mt-6 w-full rounded-full bg-pine py-4 font-bold text-cream">Sign in</button></form></section>}
