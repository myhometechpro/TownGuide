import {requireAdmin} from "@/lib/supabase/auth";import {createRentalPaymentLink} from "../actions";
export default async function Layout({children,params}:{children:React.ReactNode;params:Promise<{id:string}>}){const {id}=await params,{db}=await requireAdmin(),{data}=await db.from("vacation_rentals").select("payment_status").eq("id",id).maybeSingle();return <>{data&&data.payment_status!=="paid"&&<div className="admin-theme px-5 pt-6 lg:px-8"><form action={createRentalPaymentLink} className="mx-auto flex max-w-6xl flex-wrap items-center justify-between gap-4 border border-sand/30 bg-sand/10 p-4"><input type="hidden" name="id" value={id}/><p className="text-sm"><strong>Payment status: {data.payment_status}.</strong> Create a new one-property checkout link for a failed, canceled, or missing payment.</p><button className="bg-sand px-4 py-2 font-bold text-pine">Create replacement payment link</button></form></div>}{children}</>}
