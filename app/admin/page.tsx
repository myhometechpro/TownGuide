import Link from "next/link";import {Building2,CalendarDays,Footprints,Inbox,MapPinned,ScanLine} from "lucide-react";import {getSupabase} from "@/lib/supabase/server";import {redirect} from "next/navigation";
const sections=[[Building2,"Businesses","Add and update directory listings","/admin/businesses"],[MapPinned,"Attractions, lakes & parks","Manage free and paid places to visit","/admin/attractions"],[Footprints,"Trails","Maintain routes and visitor details","/admin/trails"],[CalendarDays,"Events","Publish verified community events","/admin/events"],[Inbox,"Listing requests","Review requests and corrections","/admin/inquiries"],[ScanLine,"QR locations","Create printable visitor QR codes","/admin/qr"]] as const;
export default async function Page(){const db=await getSupabase();if(db){const {data:{user}}=await db.auth.getUser();if(!user)redirect("/admin/login")}return <section className="min-h-screen px-5 py-12 lg:px-8"><div className="mx-auto max-w-6xl"><p className="text-xs font-black uppercase tracking-[.2em] text-forest">TownGuide administration</p><h1 className="mt-2 font-display text-4xl">Content manager</h1><p className="mt-3 text-ink/60">Keep visitor information useful, current, and straightforward.</p>{!db&&<p className="mt-6 border border-sand/40 bg-sand/20 p-4 text-sm font-bold">Preview mode: connect Supabase to manage live content.</p>}<div className="mt-8 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{sections.map(([Icon,title,text,href])=><Link href={href} className="group border border-cream/10 bg-white p-6 transition hover:border-forest" key={title}><Icon className="text-forest"/><h2 className="mt-5 font-display text-2xl">{title}</h2><p className="mt-2 text-sm leading-6 text-ink/60">{text}</p><span className="mt-5 inline-block font-bold text-forest">Manage →</span></Link>)}</div></div></section>}
