import Link from "next/link";import {BarChart3,Building2,CalendarDays,Footprints,MapPinned,MousePointerClick,Phone,ScanLine,Tags,Users} from "lucide-react";import {getSupabase} from "@/lib/supabase/server";import {redirect} from "next/navigation";
export default async function Page(){const db=await getSupabase();if(db){const {data:{user}}=await db.auth.getUser();if(!user)redirect("/admin/login")}const stats=[[Users,"Visitors","—"],[ScanLine,"QR scans","—"],[Building2,"Business views","—"],[MapPinned,"Direction clicks","—"],[MousePointerClick,"Website clicks","—"],[Phone,"Phone clicks","—"],[Tags,"Deal views","—"]] as const;return <section className="min-h-screen bg-[#eef1ed] px-5 py-10 lg:px-8"><div className="mx-auto max-w-7xl"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-xs font-black uppercase tracking-[.2em] text-forest">Admin dashboard</p><h1 className="mt-2 font-display text-4xl">This month</h1></div><Link href="/admin/qr" className="rounded-full bg-pine px-5 py-3 font-bold text-cream">Create QR location</Link></div>{!db&&<p className="mt-6 rounded-2xl bg-sand/25 p-4 text-sm font-bold">Preview mode: connect Supabase to require authentication and load live metrics.</p>}<div className="mt-7 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{stats.map(([Icon,l,v])=><article className="rounded-2xl bg-white p-5" key={l}><Icon className="text-forest"/><p className="mt-5 text-sm text-ink/55">{l}</p><p className="mt-1 text-3xl font-black">{v}</p></article>)}</div><div className="mt-6 grid gap-5 lg:grid-cols-3">{[[BarChart3,"Top QR locations"],[Building2,"Most viewed businesses"],[Footprints,"Most viewed categories"]].map(([Icon,t])=><article className="min-h-72 rounded-3xl bg-white p-6" key={String(t)}><Icon className="text-forest"/><h2 className="mt-4 font-display text-2xl">{String(t)}</h2><div className="mt-8 rounded-2xl bg-cream p-6 text-center text-sm text-ink/50">Live results appear after Supabase is connected and events are collected.</div></article>)}</div><div className="mt-6 grid gap-3 sm:grid-cols-3">{["Businesses","Attractions","Trails","Events","Deals","Inquiries"].map(x=><button className="rounded-2xl bg-white p-5 text-left font-bold" key={x}>{x}<span className="float-right">→</span></button>)}</div></div></section>}
