import type {Metadata} from "next";import {PageHero} from "@/components/page-hero";import {AttractionDirectory} from "@/components/attraction-directory";import {getAttractions} from "@/lib/content";
export const metadata:Metadata={title:"Things to Do"};export default async function Page(){const attractions=await getAttractions();return <><PageHero eyebrow="Discover Rim Country" title="Things to do" text="Find parks, scenic stops, family activities, history, recreation, and no-cost ways to enjoy the high country."/><section className="mx-auto max-w-7xl px-5 py-14 lg:px-8"><AttractionDirectory attractions={attractions}/></section></>}
