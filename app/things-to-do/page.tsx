import type {Metadata} from "next";import {PageHero} from "@/components/page-hero";import {AttractionCard} from "@/components/cards";import {getAttractions} from "@/lib/content";
export const metadata:Metadata={title:"Things to Do"};export default async function Page(){const attractions=await getAttractions();return <><PageHero eyebrow="Discover Rim Country" title="Things to do" text="Find parks, scenic stops, family activities, history, recreation, and no-cost ways to enjoy the high country."/><section className="mx-auto max-w-7xl px-5 py-14 lg:px-8"><div className="mb-8 flex flex-wrap gap-2">{["All","Parks","Lakes","Museums","Scenic","Family Activities","Historic","Recreation","Free"].map(x=><span className="rounded-full bg-white px-4 py-2 text-sm font-bold" key={x}>{x}</span>)}</div><div className="grid gap-5 md:grid-cols-2 lg:grid-cols-3">{attractions.map(x=><AttractionCard item={x} key={x.id}/>)}</div></section></>}
