import Image from "next/image";import Link from "next/link";import {notFound} from "next/navigation";import {ExternalLink} from "lucide-react";import {getAttraction} from "@/lib/content";
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params,item=await getAttraction(slug);if(!item)notFound();return <><section className="bg-pine px-5 py-14 text-cream lg:px-8"><div className="mx-auto max-w-6xl"><Link href="/things-to-do" className="text-sm font-bold text-sky">← Things to do</Link><p className="mt-8 text-xs font-black uppercase tracking-[.2em] text-sky">{item.category} · {item.costType}</p><h1 className="mt-3 max-w-4xl font-display text-4xl md:text-6xl">{item.name}</h1></div></section><main className="mx-auto grid max-w-6xl gap-10 px-5 py-14 md:grid-cols-[1.25fr_.75fr] lg:px-8"><div><div className="relative aspect-[16/10] overflow-hidden rounded-3xl"><Image src={item.image} alt={item.name} fill priority className="object-cover" sizes="(max-width:768px) 100vw, 65vw"/></div><h2 className="mt-8 font-display text-3xl">About this place</h2><p className="mt-4 text-lg leading-8 text-ink/70">{item.description}</p></div><aside className="h-fit border border-cream/10 bg-white p-6"><h2 className="font-display text-2xl">Plan your visit</h2><p className="mt-4 text-sm leading-6 text-ink/65">Access, weather, hours, fees, permits, and seasonal conditions can change. Verify current information before making a special trip.</p>{item.website&&<a href={item.website} target="_blank" rel="noreferrer" className="mt-6 inline-flex min-h-12 items-center gap-2 bg-pine px-5 font-bold text-cream">Official information <ExternalLink size={17}/></a>}</aside></main></>}
