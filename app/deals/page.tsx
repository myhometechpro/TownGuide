import type {Metadata} from "next";import {PageHero} from "@/components/page-hero";import {DealCard} from "@/components/cards";import {businesses,deals} from "@/lib/data";
export const metadata:Metadata={title:"Local Deals"};export default function Page(){return <><PageHero eyebrow="A little extra" title="Local visitor deals" text="Offers from participating businesses. All current offers in this preview are demonstrations and cannot be redeemed."/><section className="mx-auto grid max-w-7xl gap-5 px-5 py-14 md:grid-cols-2 lg:px-8">{deals.filter(x=>new Date(x.expirationDate)>=new Date()).map(x=><DealCard item={x} business={businesses.find(b=>b.id===x.businessId)} key={x.id}/>)}</section></>}
