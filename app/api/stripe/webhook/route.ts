import {createHmac,timingSafeEqual} from "node:crypto";import {NextResponse} from "next/server";import {getAdminSupabase} from "@/lib/supabase/admin";
export const runtime="nodejs";
type Session={id?:string;payment_link?:string|null;payment_status?:string;metadata?:{campaign_id?:string}};type Event={id:string;type:string;data:{object:Session}};
function verify(raw:string,header:string,secret:string){const parts=header.split(",").map(x=>x.split("=")),timestamp=parts.find(x=>x[0]==="t")?.[1],signatures=parts.filter(x=>x[0]==="v1").map(x=>x[1]);if(!timestamp||!signatures.length||Math.abs(Date.now()/1000-Number(timestamp))>300)return false;const expected=Buffer.from(createHmac("sha256",secret).update(`${timestamp}.${raw}`).digest("hex"));return signatures.some(value=>{const received=Buffer.from(value);return expected.length===received.length&&timingSafeEqual(expected,received)})}
export async function POST(request:Request){const secret=process.env.STRIPE_WEBHOOK_SECRET,signature=request.headers.get("stripe-signature"),raw=await request.text();if(!secret||!signature||!verify(raw,signature,secret))return NextResponse.json({error:"Invalid signature"},{status:400});let event:Event;try{event=JSON.parse(raw) as Event}catch{return NextResponse.json({error:"Invalid payload"},{status:400})}const db=getAdminSupabase();if(!db)return NextResponse.json({error:"Service unavailable"},{status:503});const recorded=await db.from("stripe_webhook_events").insert({event_id:event.id,event_type:event.type});if(recorded.error?.code==="23505")return NextResponse.json({received:true,duplicate:true});if(recorded.error)return NextResponse.json({error:"Unable to record event"},{status:500});if(event.type==="checkout.session.completed"||event.type==="checkout.session.async_payment_succeeded"){const session=event.data.object,successful=event.type.includes("async")||session.payment_status==="paid";if(successful){let update=db.from("ad_campaigns").update({paid:true,stripe_paid_at:new Date().toISOString(),stripe_checkout_session_id:session.id||null,updated_at:new Date().toISOString()});update=session.metadata?.campaign_id?update.eq("id",session.metadata.campaign_id):update.eq("payment_reference",session.payment_link||"");const {error}=await update;if(error)return NextResponse.json({error:"Unable to confirm campaign payment"},{status:500});const key=process.env.STRIPE_SECRET_KEY;if(key&&session.payment_link)await fetch(`https://api.stripe.com/v1/payment_links/${encodeURIComponent(session.payment_link)}`,{method:"POST",headers:{Authorization:`Bearer ${key}`,"Content-Type":"application/x-www-form-urlencoded"},body:new URLSearchParams({active:"false"}),cache:"no-store"})}}return NextResponse.json({received:true})}
