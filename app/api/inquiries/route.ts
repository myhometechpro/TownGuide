import {NextResponse} from "next/server";import {getAdminSupabase} from "@/lib/supabase/admin";
export async function POST(req:Request){try{const b=await req.json();if(!b.business_name||!b.contact_name||!b.email||String(b.email).length>254)return NextResponse.json({error:"Missing required fields"},{status:400});const payload={business_name:String(b.business_name).slice(0,200),contact_name:String(b.contact_name).slice(0,200),phone:String(b.phone||"").slice(0,50),email:String(b.email).slice(0,254),website:String(b.website||"").slice(0,500),category:String(b.category||"").slice(0,100),interest:String(b.interest||"Other").slice(0,100),message:String(b.message||"").slice(0,3000)};const db=getAdminSupabase();if(!db)return NextResponse.json({error:"Contact storage is not configured"},{status:503});const {error}=await db.from("business_inquiries").insert(payload);if(error)throw error;return NextResponse.json({ok:true})}catch{return NextResponse.json({error:"Unable to save inquiry"},{status:500})}}
