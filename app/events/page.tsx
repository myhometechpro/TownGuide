import type {Metadata} from "next";import {PageHero} from "@/components/page-hero";import {EventCard} from "@/components/cards";import {events} from "@/lib/data";
export const metadata:Metadata={title:"Events"};export default function Page(){return <><PageHero eyebrow="Around town" title="What’s happening" text="Community gatherings, markets, music, and more. Demo events are placeholders—confirm every detail with the organizer."/><section className="mx-auto max-w-5xl px-5 py-14"><div className="mb-8 flex flex-wrap gap-2">{["Today","This Weekend","This Month","Upcoming"].map(x=><span className="rounded-full bg-white px-4 py-2 text-sm font-bold" key={x}>{x}</span>)}</div><div className="grid gap-4 md:grid-cols-2">{events.filter(x=>new Date(x.startDate)>=new Date()).map(x=><EventCard item={x} key={x.id}/>)}</div></section></>}
