import type {Metadata} from "next";import {PageHero} from "@/components/page-hero";import {EventCard} from "@/components/cards";import {getEvents} from "@/lib/content";
export const metadata:Metadata={title:"Events"};export default async function Page(){const events=await getEvents();const upcoming=events.filter(x=>new Date(`${x.startDate}T23:59:59`)>=new Date());return <><PageHero eyebrow="Around town" title="What’s happening" text="Verified community gatherings, markets, music, and seasonal events will be listed here."/><section className="mx-auto max-w-5xl px-5 py-14"><div className="grid gap-4 md:grid-cols-2">{upcoming.map(x=><EventCard item={x} key={x.id}/>)}</div>{!upcoming.length&&<div className="rounded-3xl bg-white p-10 text-center"><h2 className="font-display text-3xl">No verified events listed yet</h2><p className="mt-3 text-ink/65">We&apos;ll add events after dates and organizer details are confirmed.</p></div>}</section></>}
