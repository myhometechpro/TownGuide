import type {Metadata} from "next";import {PageHero} from "@/components/page-hero";import {EventCard} from "@/components/cards";import {events} from "@/lib/data";
export const metadata:Metadata={title:"Events"};export default function Page(){const upcoming=events.filter(x=>new Date(x.startDate)>=new Date());return <><PageHero eyebrow="Around town" title="What’s happening" text="Verified community gatherings, markets, music, and seasonal events will be listed here."/><section className="mx-auto max-w-5xl px-5 py-14"><div className="mb-8 flex flex-wrap gap-2">{["Today","This Weekend","This Month","Upcoming"].map(x=><span className="rounded-full bg-white px-4 py-2 text-sm font-bold" key={x}>{x}</span>)}</div><div className="grid gap-4 md:grid-cols-2">{upcoming.map(x=><EventCard item={x} key={x.id}/>)}</div>{upcoming.length===0&&<div className="rounded-3xl bg-white p-10 text-center"><h2 className="font-display text-3xl">No verified events listed yet</h2><p className="mt-3 text-ink/65">We&apos;ll add events after dates and organizer details are confirmed.</p></div>}</section></>}
