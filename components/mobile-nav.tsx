"use client";import Link from "next/link";import {Compass,Home,Map,Menu,Tags} from "lucide-react";import {usePathname} from "next/navigation";
const items=[["Home","/",Home],["Explore","/things-to-do",Compass],["Map","/map",Map],["Deals","/deals",Tags],["More","/visitor-info",Menu]] as const;
export function MobileNav(){const path=usePathname();return <nav className="fixed inset-x-0 bottom-0 z-50 border-t border-pine/10 bg-cream/95 pb-[env(safe-area-inset-bottom)] backdrop-blur md:hidden" aria-label="Mobile navigation"><div className="grid grid-cols-5">{items.map(([label,href,Icon])=><Link key={label} href={href} className={`focus-ring flex min-h-16 flex-col items-center justify-center gap-1 text-[11px] font-bold ${path===href?"text-forest":"text-ink/65"}`}><Icon size={21}/><span>{label}</span></Link>)}</div></nav>}
