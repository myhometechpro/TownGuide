"use client";
import {useEffect,useRef} from "react";import type {Map as LeafletMap} from "leaflet";import type {MapPoint} from "@/lib/map-points";
const colors:Record<MapPoint["kind"],string>={business:"#276f76",lodging:"#9b6b43",recreation:"#39734d",event:"#a44747"};
export function TownMap({points}:{points:MapPoint[]}){const container=useRef<HTMLDivElement>(null);useEffect(()=>{let disposed=false,map:LeafletMap|undefined,observer:ResizeObserver|undefined;void import("leaflet").then(L=>{if(disposed||!container.current)return;map=L.map(container.current,{zoomControl:true,scrollWheelZoom:false}).setView([34.414,-110.57],12);L.tileLayer("https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png",{attribution:'&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a> contributors',maxZoom:19}).addTo(map);const bounds:[number,number][]=[];for(const point of points){const position:[number,number]=[point.latitude,point.longitude];bounds.push(position);const popup=document.createElement("div"),title=document.createElement("strong"),meta=document.createElement("div"),description=document.createElement("p"),link=document.createElement("a");title.textContent=point.name;meta.textContent=point.category;meta.className="town-map-popup-meta";description.textContent=point.description||point.address||"";link.textContent="View TownGuide details";link.href=point.href;link.className="town-map-popup-link";popup.append(title,meta);if(description.textContent)popup.append(description);popup.append(link);L.circleMarker(position,{radius:9,color:"#ffffff",weight:2,fillColor:colors[point.kind],fillOpacity:1}).addTo(map).bindPopup(popup)}if(bounds.length>1)map.fitBounds(bounds,{padding:[28,28],maxZoom:14});else if(bounds.length===1)map.setView(bounds[0],14);observer=new ResizeObserver(()=>map?.invalidateSize());observer.observe(container.current);window.setTimeout(()=>map?.invalidateSize(),150)});return()=>{disposed=true;observer?.disconnect();map?.remove()}},[points]);return <div><div ref={container} className="town-map h-[62vh] min-h-[440px] w-full sm:min-h-[520px]" aria-label={`Interactive TownGuide map with ${points.length} locations`}/><div className="mt-4 flex flex-wrap gap-x-5 gap-y-2 text-xs font-bold">{Object.entries(colors).map(([kind,color])=><span className="inline-flex items-center gap-2 capitalize" key={kind}><span className="size-3 rounded-full" style={{backgroundColor:color}}/>{kind}</span>)}</div>{points.length===0&&<p className="mt-4 border border-sand/30 bg-white p-4 text-sm text-ink/65">The map is ready. Pins will appear automatically as published listings receive verified coordinates.</p>}</div>}
