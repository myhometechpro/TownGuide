"use client";
export function RenewalReminderTools({business,campaign,endDate}:{business:string;campaign:string;endDate:string}){const subject="Your Heber-Overgaard advertising campaign is ending soon",message=`Hello ${business},\n\nYour ${campaign} advertising campaign is scheduled to end on ${endDate}. If you would like to continue the placement, reply to this message and we can prepare a renewal.\n\nThank you for supporting the Heber-Overgaard visitor guide.`;return <div className="flex flex-wrap gap-2"><a href={`mailto:?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(message)}`} className="border border-cream/20 px-3 py-2 text-sm font-bold">Open email</a><a href={`sms:?&body=${encodeURIComponent(message.replace(/\n+/g," "))}`} className="border border-cream/20 px-3 py-2 text-sm font-bold">Open text</a></div>}
