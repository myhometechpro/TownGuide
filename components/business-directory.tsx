"use client";import {useMemo,useState} from "react";import {Search} from "lucide-react";import {businesses} from "@/lib/data";import {BusinessCard} from "./cards";
const cats=["All","Restaurants","Coffee","Shopping","Lodging","Outdoor & Adventure","Real Estate","Home Services","Automotive","Professional Services","Other"];
export function BusinessDirectory(){const [q,setQ]=useState("");const [cat,setCat]=useState("All");const items=useMemo(()=>businesses.filter(b=>(cat==="All"||b.category===cat)&&`${b.name} ${b.shortDescription}`.toLowerCase().includes(q.toLowerCase())),[q,cat]);return <div><label className="relative block max-w-2xl"><Search className="absolute left-4 top-4 text-ink/45"/><span className="sr-only">Search businesses</span><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search local businesses" className="focus-ring h-14 w-full rounded-2xl border border-pine/15 bg-white pl-12 pr-4 text-base shadow-sm"/></label><div className="scrollbar-none mt-5 flex gap-2 overflow-x-auto pb-2" aria-label="Business category filters">{cats.map(c=><button onClick={()=>setCat(c)} key={c} className={`min-h-11 shrink-0 rounded-full px-4 text-sm font-bold ${cat===c?"bg-pine text-cream":"bg-white"}`}>{c}</button>)}</div>{items.length?<div className="mt-8 grid gap-6 md:grid-cols-2 lg:grid-cols-3">{items.map(x=><BusinessCard item={x} key={x.id}/>)}</div>:<div className="mt-10 rounded-3xl bg-white p-10 text-center"><h2 className="font-display text-2xl">No businesses found</h2><p className="mt-2 text-ink/60">Try another search or category.</p></div>}</div>}
