import {getBusinesses} from "@/lib/businesses";import {getAttractions,getEvents,getTrails} from "@/lib/content";import {canMapBusiness,cleanOptional,validLatitude,validLongitude} from "@/lib/listings";
export type MapPoint={id:string;name:string;category:string;kind:"business"|"lodging"|"recreation"|"event";description?:string;address?:string;latitude:number;longitude:number;href:string};
export async function getTownMapPoints():Promise<MapPoint[]>{const [businesses,attractions,trails,events]=await Promise.all([getBusinesses(),getAttractions(),getTrails(),getEvents()]);const points:MapPoint[]=[];for(const item of businesses){if(!canMapBusiness(item))continue;points.push({id:`business-${item.id}`,name:item.name,category:item.category,kind:item.category.toLowerCase()==="lodging"?"lodging":"business",description:cleanOptional(item.shortDescription),address:cleanOptional(item.address),latitude:item.latitude!,longitude:item.longitude!,href:`/businesses/${item.slug}`})}for(const item of attractions){const latitude=validLatitude(item.latitude),longitude=validLongitude(item.longitude);if(latitude===undefined||longitude===undefined)continue;points.push({id:`attraction-${item.id}`,name:item.name,category:item.category,kind:"recreation",description:cleanOptional(item.description),address:cleanOptional(item.address),latitude,longitude,href:`/things-to-do/${item.slug}`})}for(const item of trails){const latitude=validLatitude(item.latitude),longitude=validLongitude(item.longitude);if(latitude===undefined||longitude===undefined)continue;points.push({id:`trail-${item.id}`,name:item.name,category:item.activityType,kind:"recreation",description:cleanOptional(item.description),latitude,longitude,href:"/trails"})}for(const item of events){const latitude=validLatitude(item.latitude),longitude=validLongitude(item.longitude);if(latitude===undefined||longitude===undefined)continue;points.push({id:`event-${item.id}`,name:item.name,category:item.category,kind:"event",description:cleanOptional(item.description),address:cleanOptional(item.address),latitude,longitude,href:"/events"})}return points}
