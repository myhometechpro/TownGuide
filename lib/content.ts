import {getSupabase} from "@/lib/supabase/server";import {attractions as fallbackAttractions,trails as fallbackTrails,events as fallbackEvents} from "@/lib/data";import type {Attraction,Trail,Event} from "@/types";
export async function getAttractions(includeInactive=false):Promise<Attraction[]>{const db=await getSupabase();if(!db)return fallbackAttractions;let q=db.from("attractions").select("*").order("name");if(!includeInactive)q=q.eq("active",true);const {data,error}=await q;if(error||!data?.length)return fallbackAttractions;return data.map(r=>({id:r.id,name:r.name,slug:r.slug,category:r.category||"Recreation",description:r.description||"",image:r.image_url||"/images/town/ponderosa-forest.jpg",website:r.website||undefined,costType:(r.cost_type||"Free") as Attraction["costType"],featured:!!r.featured}))}
export async function getTrails(includeInactive=false):Promise<Trail[]>{const db=await getSupabase();if(!db)return fallbackTrails;let q=db.from("trails").select("*").order("name");if(!includeInactive)q=q.eq("active",true);const {data,error}=await q;if(error||!data?.length)return fallbackTrails;return data.map(r=>({id:r.id,name:r.name,slug:r.slug,description:r.description||"",distance:r.distance||"Varies",difficulty:(r.difficulty||"Moderate") as Trail["difficulty"],duration:r.estimated_duration||"Varies",activityType:r.activity_type||"Hiking",familyFriendly:!!r.family_friendly,image:r.image_url||"/images/town/ponderosa-forest.jpg",officialUrl:r.official_url||undefined,directionsUrl:r.directions_url||(r.latitude&&r.longitude?`https://www.google.com/maps/search/?api=1&query=${r.latitude},${r.longitude}`:undefined),latitude:r.latitude?Number(r.latitude):undefined,longitude:r.longitude?Number(r.longitude):undefined}))}
export async function getEvents(includeInactive=false):Promise<Event[]>{const db=await getSupabase();if(!db)return fallbackEvents;let q=db.from("events").select("*").order("start_date");if(!includeInactive)q=q.eq("active",true);const {data,error}=await q;if(error)return fallbackEvents;return data.map(r=>({id:r.id,name:r.name,slug:r.slug,description:r.description||"",startDate:r.start_date,time:r.start_time||"Time TBA",location:r.location||"Heber-Overgaard",category:r.category||"Community",image:r.image_url||"/images/town/ponderosa-forest.jpg"}))}
